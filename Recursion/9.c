#include<stdio.h>

/*   to reverse a string using recursion. >
Test Data :
Input any string: w3resource
Expected Output:

The reversed string is: ecruoser3w 
 */
int reverse(char* str,size)

int main(){
  char str[100];
  fgets(str,sizeof(str),stdin);
  reverse(str);
}